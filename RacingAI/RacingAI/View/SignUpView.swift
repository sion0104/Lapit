import SwiftUI

struct SignUpView: View {
    @Binding var identifier: String
    
    var body: some View {
        VStack {
            
        }
    }
}

#Preview {
    SignUpView(identifier: .constant("id"))
}
